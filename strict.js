'use strict';
const city = 'town';
console.log(city);

const name = 'Win10';
console.log(name);

const server1 = 'kdjfkdjf';
const server2 = 'iejfeife';

console.log(server1);
console.log(server2);


const firstTotal = 5;
let subtotal = 0;
let total = 0;

subtotal = firstTotal;
total = subtotal;

console.log('total: ' + total);
console.log('subtotal: ' + subtotal);

const serverName = 'Windows 2016 Server';
console.log('server: ' + serverName);

const days = ['Sunday', 'Monday', 'Wednesday'];
console.log('days: ' + days.join(', '));

let person = {};

person = {
	firstName: 'Harry',
	lastName: 'Hopkins',
	'first-role': 'unknown',
	'second-role': 'missing'
};
console.log('person: ', person);

